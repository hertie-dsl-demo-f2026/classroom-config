<!-- SYSTEM-OWNED: this README, and the workflows under .github/, are regenerated by every "Bootstrap cohort" run, and by the nightly Refresh, so keep notes elsewhere - edits here will be overwritten. Every OTHER non-sample file in this repo is yours: the engine never rewrites students.csv, schedule.yml, people.yml, teams.csv or grades/*.csv once they exist. -->

# classroom-config - this cohort's private config

**PRIVATE.** 

This is the entire per-cohort data hub - roster, teams, grades, schedule, and this cohort's own instructors/TAs. No PII (emails, ids, names) leaves this repo. 

Instructors/TAs edit these files; the workflows in the **course org's** Actions tab read them. 


**Live files vs samples.** 
Every file you edit here ships as a pair: 
- `<file>` is the live file, read by the engine. 
- `<file>.sample` is pure documentation: the engine never reads a `.sample`, and every bootstrap and nightly refresh re-pushes them so they can never go schema-stale. 

## students.csv - the roster (required)

One row per student. Leave `github_handle`, `github_id`& `enrol_code` blank - the pipeline fills them in.

| column | filled by | notes |
|--------|-----------|-------|
| student_id | instructor | institutional id |
| hertie_email | instructor | **match key** - enrolment reconciles on this |
| name | instructor | display name |
| github_handle | onboarding | blank until they join via the welcome "Join" issue |
| github_id | onboarding | numeric id captured on join - **immutable; never hand-edit** |
| section | instructor | optional grouping (e.g. A/B) |
| enrol_code | **Send enrolment codes** | random non-PII token, emailed to the student; they paste it into the "Join" issue. Leave blank - the button fills blanks only, never rewrites an issued code |
| role | instructor | `enrolled` (blank means enrolled) or `auditor` - auditors are read-only: released materials only, no assignment repos, no gradebook, no project teams |

A push to this file triggers **Sync membership** automatically, reconciling the `students` and `auditors` teams to match (a deleted row revokes access on that same push).

## `grades/<assignment>.csv` - marks (optional, when returning grades)

One file per assignment,

The autograder pre-fills `auto`/`team_grade` from hidden tests, creating the file if it doesn't exist; faculty & instructors fill the rest, then the following workflows:
```
Sync gradebooks -> Render grades -> Distribute grades
```
It runs itself **once** per assignment, at that
assignment's grading deadline in `schedule.yml`

For an all-manual assignment (`autograde: false`), copy the sample's header into `grades/<slug>.csv` and fill it yourself. 

> ### Changing grades:
>
>`auto`/`team`/`team_grade` are **write-once**: once filled, no run overwrites them, so your corrections stand. 
>
>To recompute, clear those cells and delete `autograde/<slug>/`. 

### What reaches students, when:
A generated, read-only `cohort-gradebook.csv` (one row per student, one column-group per assignment) appears alongside the per-student gradebooks on every **Render grades** - never hand-edit it, it's a glance view, not a source. **Nothing in `grades/`,`autograde/` or the gradebooks reaches a student until the separate Distribute grades
button** - autograding and your review happen entirely in this private repo.

## teams.csv - group membership (optional, for group assignments)

2 possible ways this is populated:
1. Students self-select via the welcome "Join team" issue, which appends rows here, 
2. Instructors edit directly - a push here also triggers **Sync membership**. 

## schedule.yml - the release plan + due dates + events (optional)

This cohort's whole schedule in one file. Changing a date later is just committing the edit to this file on main (GitHub web UI, or clone/commit/push).

## people.yml - this cohort's instructors/TAs (optional)

Most cohorts have different lecturers/TAs, so - unlike course admins (course-org level) - instructors/TAs are declared here, per cohort. **Sync membership**
reconciles them into this cohort's own `instructors` team AND a course-org level `instructors-<tag>` team (push access scoped to just this year's content repos, plus
the central `.github` repo so they can use the workflows too). It also supplies this cohort's website staff cards. 
