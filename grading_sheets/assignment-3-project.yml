# -------------------------------------------------------------------------------------------
# GRADING SHEET · assignment-3-project · Group project · INSTRUCTOR-OWNED
#
# From grading_config.yml / schedule.yml (edit THERE, not here):
#   group assignment · 50 points (Report 20, Code 20, Presentation 10) · autograde on
#   due Fri 23 Oct 2026 23:59 · late work to Mon 26 Oct 2026 23:59 at 10%/day
# Status: OPEN - 0 of 4 teams have submitted
#
# Auto-filled by the toolkit (you never type these): every `info:` block. `submitted` and
# `days_late` fill at the due date and refresh after each late push; `contributions` is read
# from CONTRIBUTIONS.md at the same moments; `autograde` fills once at the cutoff. All of them
# freeze at the cutoff.
# You fill in: score_group (one value per question; max points shown beside each),
# feedback_group, adjustment_individual, feedback_individual, notes_not_shared_with_students.
# Anything you type is never touched. Nothing reaches a student until you run Distribute
# grades. YAML comments you add are not preserved when the toolkit rewrites the file.
# -------------------------------------------------------------------------------------------
teams:
  team-alpha:
    info:                       # toolkit-owned, shown for information only - nothing is declared here
      submitted:
      days_late:
      contributions:
      autograde:
    score_group:                # yours: the question names and maxima come from grading_config.yml
      Report:                   # /20
      Code:                     # /20
      Presentation:             # /10
    feedback_group:
    members: {}
  team-beta:
    info:                       # toolkit-owned, shown for information only - nothing is declared here
      submitted:
      days_late:
      contributions:
      autograde:
    score_group:                # yours: the question names and maxima come from grading_config.yml
      Report:                   # /20
      Code:                     # /20
      Presentation:             # /10
    feedback_group:
    members: {}
  team-gamma:
    info:                       # toolkit-owned, shown for information only - nothing is declared here
      submitted:
      days_late:
      contributions:
      autograde:
    score_group:                # yours: the question names and maxima come from grading_config.yml
      Report:                   # /20
      Code:                     # /20
      Presentation:             # /10
    feedback_group:
    members: {}
  team-zeta:
    info:                       # toolkit-owned, shown for information only - nothing is declared here
      submitted:
      days_late:
      contributions:
      autograde:
    score_group:                # yours: the question names and maxima come from grading_config.yml
      Report:                   # /20
      Code:                     # /20
      Presentation:             # /10
    feedback_group:
    members:
      henrycgbaker:
        adjustment_individual:
        feedback_individual:
        notes_not_shared_with_students:
  team-test:
    info:                       # toolkit-owned, shown for information only - nothing is declared here
      submitted:
      days_late:
      contributions:
      autograde:
    score_group:                # yours: the question names and maxima come from grading_config.yml
      Report:                   # /20
      Code:                     # /20
      Presentation:             # /10
    feedback_group:
    members:
      henrycgbaker:
        adjustment_individual:
        feedback_individual:
        notes_not_shared_with_students:
  test:
    info:                       # toolkit-owned, shown for information only - nothing is declared here
      submitted:
      days_late:
      contributions:
      autograde:
    score_group:                # yours: the question names and maxima come from grading_config.yml
      Report:                   # /20
      Code:                     # /20
      Presentation:             # /10
    feedback_group:
    members:
      henrycgbaker:
        adjustment_individual:
        feedback_individual:
        notes_not_shared_with_students:
